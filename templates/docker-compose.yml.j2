version: "3.7"
services:
  impacta_app:
    image: "{{ docker_image_name }}:{{ docker_image_version }}"
    ports:
      - "5000:5000"
    environment:
      - REDIS=redis
  redis:
    image: "redis:alpine"
